language: c

compiler:
  - gcc
  - clang

before_install:
  - sudo apt-get update >/dev/null
  - sudo apt-get -q install check libprotobuf-c0-dev protobuf-c-compiler

before_script:
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - ctest
